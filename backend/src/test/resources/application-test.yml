spring:
  datasource:
    url: jdbc:h2:mem:careerpath-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  security:
    jwt-secret: test-only-jwt-secret-with-at-least-thirty-two-characters
